<div class="container">
  <div class="row">
      <h1 class="mt-3 mb-5 text-primary font-weight-light">Find the place you need to go!</h1>
      <div class="mb-4 col-12">
        <a class="btn btn-outline-primary"href="/find-places/restaurants">Restaurants</a>
        <a class="btn btn-outline-primary"href="/find-places/drink-dance">Drink and dance</a>
        <a class="btn btn-outline-primary"href="/find-places/useful">Useful</a>
      </div>
    {{#each places}}
      <div class="card col-4 border-0 my-2">
          {{!-- <img src={{this.image_url}} class="card-img-top mx-auto" style="width: 20%"> --}}
        <div class="card-body border rounded shadow-sm">
          <h5 class="card-title text-primary">{{this.name}}</h5>
          <h6 class="card-subtitle mb-2 text-muted">{{this.address}}</h6>
          <p class="card-text">{{this.city}} {{this.zip}}</p>
          <p class="card-text">{{this.description}}</p>
          <a class="card-text" href="{{this.link}}">{{this.link}}</a>
          <p class="card-text text-primary">{{this.category}}</p>
        </div>
        {{#ifEquals ../user.role "ADMIN"}}
        <div class="row p-2">
          <a class="btn btn-outline-primary col mx-2" href="/edit-place/{{this._id}}">Edit</a>
          <a class="btn btn-outline-danger col mx-2" href="/delete-place/{{this._id}}">Delete</a>
        </div>
        {{/ifEquals}}
      </div>
    {{/each}}  
  </div>
</div>
